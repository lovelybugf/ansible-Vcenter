---
ansible-navigator:
  execution-environment:
    container-engine: podman
    enabled: true
    image: quay-ssc2.local.amigo.com/admin/nampd-vmware-ee:v1.2
    pull:
      policy: missing
  mode: stdout

  playbook-artifact:
    enable: false
...
